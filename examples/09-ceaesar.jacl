let factor = 5;

let text = buf 10;
let text_len = read(0, text, 10);

let text_end = + text text_len;
let ptr = text;

while < ptr text_end {
    unless == !ptr 0 {
        !ptr = + !ptr factor;
    }
    ptr = + ptr 1;
}

printn(text, 10);
